{{-- <x-filament::page>
    <div class="w-1/4 p-4 text-white bg-gray-800">
        <h2 class="mb-4 text-xl font-bold">Available Users</h2>
        <ul class="space-y-2">
            @foreach ($users as $user)
                <li>
                    <a href="{{ route('conversation', $user->id) }}" class="text-blue-500 hover:underline">{{ $user->name }}</a>
                </li>
            @endforeach
        </ul>
    </div>

</x-filament::page> --}}
